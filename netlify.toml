[build]
  base = "frontend"
  publish = "dist"
  command = "echo 'Using pre-built dist folder'"

# SPA fallback - all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
